[package]
name = "s3-downloader"
version = "0.1.0"
edition = "2024"
description = "A small macOS desktop app for downloading S3 objects with the AWS CLI."

[dependencies]
gpui = { version = "0.2.2", features = ["runtime_shaders"] }
gpui-component = "0.5.1"
gpui-component-assets = "0.5.1"
serde_json = "1"
